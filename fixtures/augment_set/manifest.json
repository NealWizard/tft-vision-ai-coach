{
  "schema_version": "1.0.0",
  "dataset": "augment_set",
  "description": "Ground-truth augment offer recognition fixtures.",
  "version": "0.1.0",
  "patch_scope": ["set17-*"],
  "sample_count": 0,
  "label_fields": ["slot", "augment_id", "tier"],
  "samples_dir": "samples",
  "naming_rule": "{patch}_{resolution}_{stage}_{seq}.png + .json sidecar"
}
